---
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Stardust particle reactor — opens Nebula X in reactor palette." />
    <meta name="theme-color" content="#000000" />
    <title>Stardust — Particle reactor</title>
    <script defer src="/_vercel/insights/script.js"></script>
    <script is:inline>
      (function () {
        var sent = false;
        function go() {
          if (sent) return;
          sent = true;
          location.replace('/nebula-x/index.html?palette=reactor');
        }
        window.addEventListener('load', function () {
          setTimeout(go, 400);
        });
        setTimeout(go, 2500);
      })();
    </script>
  </head>
  <body style="background:#000;color:#ffffff;font-family:sans-serif;padding:2rem">
    <p>
      Opening the particle reactor.
      <a href="/nebula-x/index.html?palette=reactor" style="color:#f59e0b">Continue</a>
    </p>
  </body>
</html>
