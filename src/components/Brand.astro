---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={className}><span class="text-white">Colin</span><span class="text-cyan-400">Builds</span></span>
