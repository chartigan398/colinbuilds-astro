---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={className}><span class="brand-colin">Colin</span><span class="brand-builds">Builds</span></span>
