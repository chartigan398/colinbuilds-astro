{
  "name": "colinbuilds-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "clean": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('.astro',{ recursive:true, force:true });\"",
    "pack:nebula-x": "node scripts/pack-nebula-x.mjs",
    "prebuild": "npm run clean && npm run pack:nebula-x",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^5.18.2",
    "tailwindcss": "^4.3.2"
  }
}
